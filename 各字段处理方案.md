# 方案：
feature_sub_system_gaia表中的title字段：
原内容：[RAN-6911556][【阿联酋】SSB 功率boosting基站侧配置（无信令影响）]-盖亚创建
处理结果：[RAN-6911556][【阿联酋】SSB 功率boosting基站侧配置（无信令影响）]
去掉“-盖亚创建”内容

# 特性域变更分析结果：(一个方案一个)
该字段为表结构，从root_feature_change_analysis_sub表中检索rdc_id，然后提取下面的5个字段进行建表
feature_name   feature_change_type   usecase_name   usecase_change_type   change_point

# 需求描述:(一个方案一个)
feature_sub_system_gaia表中的description字段

# 算法描述：(一个方案一个)
feature_sub_system_gaia表中的`algorithm`字段
字段为文本则直接取文本中的内容
如果算法思路为icenter算法链接，则取算法方案中：
1 需求分析
2 算法概要设计的全部内容；

# 实现思路：(一个方案一个)
feature_sub_system_gaia表中的`implementation`字段

# 波及子系统：(一个方案多个)
root_system_change_analysis_sub 的 sub_system_name 字段

# 波及组件(一个方案多个，按照子系统分)
root_in_sub_system_process_sub中的source_system与aim_system

还需change_type change_describe business_flow用于标记
不涉及meta_object_subsystem中的subsystem_name
# 波及模块（好像只有SPA子系统的才有这个）
root_component_main
发送方 接收方 业务流 业务流输出策略 业务流变更类型 备注
sender receiver business_flow output_strategy change_type remarks


两行一模一样，就工作流不一样（这是个问题，如何展示？）
# 波及的变更点(一个方案多个，按照子系统分)
root_system_change_analysis_sub 的 system_change_describe 